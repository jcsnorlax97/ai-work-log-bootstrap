interface:
  display_name: "Daily Capture Review"
  short_description: "Review daily captures before promotion"
  default_prompt: "Use $daily-work-log to generate today's daily capture review from the private AI work-log vault."
